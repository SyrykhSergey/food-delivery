import React from 'react';
import {MenuBoard} from '../../../widgets/MenuCatalog'

const HomePage = () => {
    return (
        <div>
            <MenuBoard />
        </div>
    );
};

export default HomePage;